# HIGH RISK INCIDENT — prod-database

> **IMMEDIATE ACTION REQUIRED — Agent did NOT auto-fix this**

---

## ERROR DETAILS

| Field | Value |
|-------|-------|
| **Resource** | `prod-database` |
| **Alert Condition** | `iam_policy_change` |
| **GCP Project** | `poc-genai-chatbot` |
| **Alert Started** | `2026-03-27T05:46:34.912565` |
| **PR Created** | `2026-03-27 05:46:39 UTC` |
| **Risk Level** | HIGH |
| **Auto-Fixed** | NO — requires human judgement |
| **GCP Changes Made by Agent** | ZERO — nothing was touched |

---

## WHAT BROKE

> **Unusual IAM permission change — owner role granted on production database**

---

## WHY THE AGENT DID NOT AUTO-FIX THIS

The agent never automatically handles HIGH RISK situations.
The following categories are always escalated to a human:

| Risk Category | Why It Is Dangerous |
|--------------|---------------------|
| IAM or permission changes | Could indicate unauthorized access or privilege escalation |
| Production databases | Auto-actions risk data loss or corruption |
| Firewall rule changes | Could expose your infrastructure to the internet |
| Billing anomalies | Could indicate compromised account or runaway costs |
| Credentials or secrets | Security-sensitive — needs human judgement only |

This alert matched: condition `iam_policy_change` on resource `prod-database`

---

## GEMINI AI — EXACT SOLUTION

> This solution was generated by Gemini AI specifically for this incident.
> Follow these steps exactly to resolve the issue.

Okay, I understand. A HIGH RISK alert triggered due to an unusual IAM permission change granting the owner role on the production database `prod-database` in project `poc-genai-chatbot`. Here's a solution guide:

**Immediate Actions (do these first):**

1.  **Disable the compromised account:** If you suspect an account is compromised, immediately disable it to prevent further damage.
2.  **Audit Logs:** Immediately start reviewing audit logs for the last hour to identify the source of the IAM change and any other actions taken by the account.
3.  **Isolate the Database:** If possible, temporarily isolate the database from external access to prevent unauthorized data access or modification.

**Investigation Commands:**

```bash
gcloud logging read "resource.type=gcloud.googleapis.com/Project AND resource.labels.project_id=poc-genai-chatbot AND protoPayload.methodName='SetIamPolicy' AND protoPayload.resourceName='prod-database'" --limit=20
gcloud audit-logs read --service="cloudresourcemanager.googleapis.com" --method="SetIamPolicy" --resource="prod-database" --project="poc-genai-chatbot" --limit=20
```

**Fix Steps:**

1.  **Remove the Owner Role:** Revoke the owner role from the unauthorized user/service account. Replace `<compromised-account>` with the actual email address.

```bash
gcloud projects get-iam-policy poc-genai-chatbot --format="yaml" > temp_policy.yaml
# Edit temp_policy.yaml to remove the compromised account from the roles/owner binding.
# Example:
# bindings:
# - members:
#   - user:<compromised-account> # REMOVE THIS LINE
#   role: roles/owner
gcloud projects set-iam-policy poc-genai-chatbot temp_policy.yaml
rm temp_policy.yaml
```

**Verification:**

Verify that the owner role has been removed from the unauthorized account.

```bash
gcloud projects get-iam-policy poc-genai-chatbot
```

Check the output to ensure the compromised account is no longer listed under the `roles/owner` binding.


---

## GCP CONSOLE LINKS — INVESTIGATE HERE

Open these links directly — no searching required:

| What to Check | Direct Link |
|--------------|-------------|
| Resource Overview | https://console.cloud.google.com/run/detail/us-central1/prod-database?project=poc-genai-chatbot |
| Recent Error Logs | https://console.cloud.google.com/logs/query;query=resource.labels.service_name%3D%22prod-database%22%20severity%3E%3DWARNING?project=poc-genai-chatbot |
| IAM and Permissions | https://console.cloud.google.com/iam-admin/iam?project=poc-genai-chatbot |
| Audit Logs | https://console.cloud.google.com/logs/query;query=logName%3D%22projects%2Fpoc-genai-chatbot%2Flogs%2Fcloudaudit.googleapis.com%2Factivity%22?project=poc-genai-chatbot |
| Billing Overview | https://console.cloud.google.com/billing?project=poc-genai-chatbot |
| All Active Alerts | https://console.cloud.google.com/monitoring/alerting?project=poc-genai-chatbot |

---

## STEP-BY-STEP ACTION CHECKLIST

### Step 1 — Understand What Happened
- [ ] Open the Recent Error Logs link above
- [ ] Read the last 20 error lines
- [ ] Open Audit Logs — identify who or what triggered this
- [ ] Note the exact timestamp and correlate with recent deployments

### Step 2 — Contain the Issue Based on Alert Type

**If IAM change — find and revert the suspicious binding:**

```bash
# See all current IAM bindings
gcloud projects get-iam-policy poc-genai-chatbot

# Remove suspicious binding
gcloud projects remove-iam-policy-binding poc-genai-chatbot \
  --member='SUSPICIOUS_MEMBER' \
  --role='SUSPICIOUS_ROLE'
```

**If billing spike — identify the resource causing cost:**

```bash
gcloud services list --enabled --project=poc-genai-chatbot
gcloud run services list --region=us-central1 --project=poc-genai-chatbot
```

**If firewall change — review and revert suspicious rules:**

```bash
gcloud compute firewall-rules list --project=poc-genai-chatbot
gcloud compute firewall-rules delete RULE_NAME --project=poc-genai-chatbot
```

**If database issue — check connections and disk:**

```bash
gcloud sql instances list --project=poc-genai-chatbot
gcloud sql operations list --instance=INSTANCE_NAME --project=poc-genai-chatbot
```

**If Cloud Run service issue — check status and logs:**

```bash
gcloud run services describe prod-database --region=us-central1 --project=poc-genai-chatbot
gcloud run services logs read prod-database --region=us-central1 --project=poc-genai-chatbot --limit=50
```

### Step 3 — Apply the Fix
- [ ] Follow the Gemini solution section above
- [ ] Verify the alert cleared in GCP Monitoring
- [ ] Confirm service is healthy

### Step 4 — Close This PR
- [ ] Add a comment describing what you found and what you did
- [ ] Close this PR without merging — this is an incident report

---

## FULL ALERT DATA

<details>
<summary>Expand to see raw alert JSON</summary>

```json
{'incident': {'summary': 'Unusual IAM permission change — owner role granted on production database', 'condition_name': 'iam_policy_change', 'resource_name': 'prod-database', 'scoping_project_id': 'poc-genai-chatbot', 'state': 'open', 'started_at': '2026-03-27T05:46:34.912565'}, 'metric_value': None}
```

</details>

---

*Auto-created by GCP SRE Agent — escalated because this is HIGH RISK*
*Zero changes were made to your GCP infrastructure*